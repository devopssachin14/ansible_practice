---
# me: install httpd:
- name: install httpd
  ansible.builtin.yum: 
    name: httpd
    state: present
  notify: start-httpd

- name: copy index.html /var/www/html
  ansible.builtin.copy: 
    src: index.html
    dest: "{{ index_dest }}"
